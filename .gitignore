# Dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build
/dist

# Python
__pycache__/
*.pyc

# Misc
.DS_Store
*.pem
Thumbs.db
.nano-banana-config.json

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.log

# Local env files
.env*.local
.env
.env.development
.env.production

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Bun
bun.lockb

# IDE
.idea/
.vscode/
*.swp
*.swo

# Test files
/test-pdfs/
*.test.pdf

# Patient PDFs (may contain real patient data)
docs/test-pdfs/**/*.pdf
!docs/test-pdfs/originals/referral_dummy.pdf
!docs/test-pdfs/originals/consent_form_real_sample.pdf
!docs/test-pdfs/referrals/*.pdf
!docs/test-pdfs/addressees/*.pdf
!docs/test-pdfs/results/*.pdf
!docs/test-pdfs/letter-subtypes/*.pdf
!docs/test-pdfs/review-referrals/*.pdf
!docs/test-pdfs/urgent/*.pdf

# Real BJC HL7 / PDF samples — local reference only, NEVER commit
/samples/

# Local artifacts (email attachments, scratch bundles)
*.zip

# Claude Code local state (settings, worktrees with vendored node_modules)
.claude/

# Supabase local
supabase/.temp/

# AWS Amplify
amplify-builds/
aws-exports.js

# Explainer video pipeline (Remotion subproject)
scripts/video/remotion/node_modules/
scripts/video/remotion/package-lock.json
scripts/video/remotion/.remotion/

# Login instructional video — rendered output is committed (Amplify needs it
# at deploy time and we don't have a regen-on-build pipeline). Re-render via
# `npx remotion render` in scripts/video/remotion/ when narration changes.

# Old narration segment archives (kept locally, not in repo)
scripts/video/remotion/public/narration-segments/_archive/
scripts/video/remotion/public/narration-segments/_archive2/

# Raw video render output (final committed copy lives in public/)
/output/

# Playwright MCP debug artifacts
.playwright-mcp/

# Local UI test screenshots
/login-mobile.png
/login-with-video.png
/conversion-options-after.png

# BJC test PDFs from Nicole (real PHI — never commit)
docs/latest/
docs/urgent test.pdf
docs/urgent test 2.pdf

# Separate-client strategy brief — keep out of the BJC repo
docs/strategy/advanced-hearing-solutions-backoffice-automation.md

# whiteboard rehearsal video (local only)
/output/
/scripts/whiteboard-video/
